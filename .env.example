# Copy this file to .env (gitignored) for local testing, or export the
# variable directly in your shell / MCP client config.
#
# Create this token in HubSpot: Settings -> Integrations -> Private Apps
# -> Create a private app. Required scopes:
#   crm.objects.contacts.read   crm.objects.contacts.write
#   crm.objects.deals.read      crm.objects.deals.write
#   crm.objects.tickets.read    crm.objects.tickets.write
HUBSPOT_ACCESS_TOKEN=
